# Settings

## Organization

Each distinct settings group = its own options-tree node.
Why: discoverable, searchable, consistent. Hidden groups get missed.

Avoid: in-pane selectors that hide sibling groups behind one node — dropdown, tab strip (TNotebook/TPageControl/TTabControl), radio switch. Split into nodes instead.

